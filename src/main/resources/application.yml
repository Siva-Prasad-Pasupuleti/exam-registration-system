spring:
  datasource:
    url: jdbc:postgresql://dev-postgres.choyusqw2vhj.eu-west-1.rds.amazonaws.com:5432/appdb
    username: appadmin
    password: "[!3m*yvo4MH$r$u}wmz1"

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update

    show-sql: true

server:
  port: 8080
